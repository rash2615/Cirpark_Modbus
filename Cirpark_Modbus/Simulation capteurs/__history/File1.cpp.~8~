#pragma hdrstop
#pragma argsused

#ifdef _WIN32
#include <tchar.h>
#else
  typedef char _TCHAR;
  #define _tmain main
#endif

#include <stdio.h>
#include "SNClientTCP.h"

 int _tmain(int argc, _TCHAR* argv[]) 
{
SNClientTCP session1;

bool connect = session1.SeConnecterAUnServeur("172.20.21.221",3306);
if (connect == true)
 return 1;
}

